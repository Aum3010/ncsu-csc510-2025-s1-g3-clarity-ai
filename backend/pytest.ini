[pytest]
markers =
    skip_supertokens: Skip tests that require SuperTokens API methods that don't exist
    skip_auth: Skip tests with authentication issues
    skip_integration: Skip integration tests with known issues
